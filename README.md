## 📦 模型权重下载 (Pretrained Weights)

本项目的运行依赖预训练的 U-Net 分割模型。请下载模型权重并将其放置在 `weights/` 目录下：

- **GitHub Release 下载 (推荐)**: [点击下载 best_model_weights.pth]()

确保文件目录为以下样式：
TartVision/
├── weights/
│   └── best_model_weights.pth
├── tart_vision/
...
